<Window x:Class="Segment.App.Views.NotificationToast"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotificationToast" Height="170" Width="360"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" Topmost="True"
        ShowActivated="False"
        WindowStartupLocation="Manual"
        KeyDown="Window_KeyDown">

    <Border CornerRadius="10" Background="#252526" BorderBrush="#3E3E42" BorderThickness="1" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Save as preferred translation?" Foreground="#FFD700" FontWeight="Bold" FontSize="13"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock x:Name="OldTermText" Foreground="#CCCCCC" FontSize="13"/>
                <TextBlock Text="  ->  " Foreground="#888888" FontSize="13"/>
                <TextBlock x:Name="NewTermText" Foreground="#4CC2FF" FontSize="13" FontWeight="SemiBold"/>
            </StackPanel>

            <TextBlock Grid.Row="2" Text="A: Always, P: This project, N/Esc: Not now" Foreground="#666666" FontSize="10" Margin="0,8,0,0"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="AlwaysButton" Content="_Always" Width="86" Height="26" Margin="0,0,8,0" Click="Always_Click" IsDefault="True"/>
                <Button Content="This _project" Width="95" Height="26" Margin="0,0,8,0" Click="Project_Click"/>
                <Button Content="_Not now" Width="84" Height="26" Click="NotNow_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
